// /** @type {import('next').NextConfig} */
// const nextConfig = {};

import next from 'next';

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
   reactStrictMode: false,
  output: "export",
};

export default nextConfig;